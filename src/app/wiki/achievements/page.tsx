import { AchievementsPage } from "@/page/wiki/AchievementsPage";
import { createMetadata } from "@/seo/metadata";
import { pageTdk } from "@/seo/tdk";

export const metadata = createMetadata(pageTdk.achievements);
export default AchievementsPage;
